rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Ground level safety
    match /{document=**} {
      allow read, write: if false;
    }

    // Counters collection - allow public access for diagnosis tracking
    match /counters/{counterId} {
      allow read, write: if true;
    }
  }
}
